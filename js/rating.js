let categorias; // Obtener todos los tabs de categorías del DOM, y guardarlos en la variable categorias
let articulos; // Obtener todos los artículos del DOM, y guardarlos en la variable articulos

// Cuando el DOM se carga por completo
document.addEventListener ("DOMContentLoaded", function () {
	//Asegúrense de convertir los números de rating en estrellas activas e inactivas
})

// Al hacer clic en cada tab/categoría
categorias.forEach(function(categoria) {
	categoria.addEventListener ("click", function () {
		//Asegúrense de convertir los números de rating en estrellas activas e inactivas
    })
})
